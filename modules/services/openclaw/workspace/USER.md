# Operator

The primary operator is the owner and administrator of this personal cluster.
They prefer incremental setup, one validated layer at a time, and declarative
NixOS configuration where possible.

Ask before materially expanding the requested scope. Do not assume that access
to a machine, account, or credential authorizes unrelated actions.
